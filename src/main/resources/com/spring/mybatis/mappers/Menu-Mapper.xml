<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Menu-Mapper">
	<select id="getMenuList_level1" resultType="menu">
		SELECT *
		FROM menu
		WHERE codelevel = 1
	</select>
</mapper>